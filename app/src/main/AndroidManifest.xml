<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="com.example.libib">

	<application
		android:allowBackup="true"
		android:icon="@mipmap/ic_launcher"
		android:label="@string/app_name"
		android:roundIcon="@mipmap/ic_launcher_round"
		android:supportsRtl="true"
		android:theme="@style/Theme.Libib">
		<activity android:name=".IssuedBooksActivity"></activity>
		<activity android:name=".ReturnBookActivity" />
		<activity android:name=".IssueBookActivity" />
		<activity android:name=".RemoveBookActivity" />
		<activity android:name=".UpdateBookActivity" />
		<activity android:name=".AddBookActivity" />
		<activity android:name=".AllBooksActivity" />
		<activity android:name=".AdminHomeActivity" />
		<activity android:name=".WelcomeActivity" />
		<activity android:name=".SplashActivity">
			<intent-filter>
				<action android:name="android.intent.action.MAIN" />

				<category android:name="android.intent.category.LAUNCHER" />
			</intent-filter>
		</activity>
	</application>

</manifest>